---
phase: 01-preprocessing
plan: 01-03
type: execute
---

<objective>
Reconstruct Reddit discussion threads by linking each comment to its parent post and building a thread context string for each record.

Purpose: Comments about AI in healthcare may not mention those topics directly — they are relevant because they respond to a parent post that does. The LLM relevance classifier (Plan 01-04) must receive parent context alongside the comment text. This plan builds that context field.
Output: `data/processed/02_with_thread_context.rds` — all records augmented with `thread_id`, `parent_text`, and `full_context` fields.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-preprocessing/01-02-SUMMARY.md
@R/utils.R
@config/config_template.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/thread_reconstruct.R — parent linking and thread ID assignment</name>
  <files>R/thread_reconstruct.R</files>
  <action>
    Create `R/thread_reconstruct.R` with a function `reconstruct_threads(df)`.

    **Background on `parent_id` and `post_id` in this dataset:**
    - The data uses a proprietary crawler schema — IDs in `parent_id` are already bare (no `t3_/t1_` Reddit prefixes). Do NOT apply prefix-stripping.
    - `post_id` (mapped from `parsedPostId`) directly identifies the root post for every comment — this means `thread_id` can be assigned directly without chain-walking for most records.
    - Root posts have `parent_id == NA`.

    **Implementation:**
    1. Do NOT strip any prefixes — `parent_id` values are already clean bare IDs.
    2. For each record, derive `thread_id`:
       - For posts (`type == "post"`): `thread_id = id`
       - For comments: `thread_id = post_id` (use `parsedPostId`-derived column directly — no chain walk needed for thread assignment)
    3. Build a lookup of `id → title + text` for posts and `id → text` for comments.
    4. For each record, add:
       - `parent_text` (character): direct parent's text looked up via `parent_id` → lookup table (NA for root posts or when parent not in dataset)
       - `root_post_text` (character): root post's combined title + body, looked up via `post_id` → lookup table (same as the record's own text for posts)
       - `thread_depth` (integer): 0 for posts; for comments, compute depth by walking the `parent_id` chain iteratively with a lookup table (id → parent_id). Cap at 10 levels to guard against malformed data. Note: `thread_id` uses `post_id` directly and does NOT require chain-walking.
    5. Log: number of records with resolved parent, number with unresolved parent (parent not in dataset — these were legitimately filtered in 01-02 and their absence is expected)
    6. Return augmented tibble

    **Edge cases to handle:**
    - `parent_id` is NA → root post, skip parent lookup
    - Parent ID not found in dataset (filtered out earlier) → set `parent_text = NA`, `root_post_text = NA`, `thread_depth = NA_integer_`. These will still go through relevance classification with their own text only.
    - Circular references (malformed data) → detect with cycle check, assign `thread_depth = NA_integer_`
  </action>
  <verify>`source("R/thread_reconstruct.R"); df_threaded <- reconstruct_threads(df_filtered); "parent_text" %in% names(df_threaded); "thread_id" %in% names(df_threaded)`</verify>
  <done>All records have thread_id, parent_text, root_post_text, and thread_depth fields; posts have thread_depth == 0; direct replies have thread_depth == 1; log reports resolution rate</done>
</task>

<task type="auto">
  <name>Task 2: Build full_context field and run thread reconstruction pipeline script</name>
  <files>R/thread_reconstruct.R (updated), scripts/02_thread_reconstruct.R</files>
  <action>
    Add a function `build_full_context(df)` to `R/thread_reconstruct.R` that creates a `full_context` character field for each record — this is what the LLM will receive for relevance classification.

    **Context composition rules:**
    - For root posts (`thread_depth == 0`):
      `full_context = paste0("[POST in r/", subreddit, "]\nTitle: ", title, "\n\n", text)`
    - For direct comments (`thread_depth == 1`, parent is a post):
      `full_context = paste0("[COMMENT in r/", subreddit, " replying to post]\nParent post title: ", parent_title, "\nParent post body: ", truncate(root_post_text, 500), "\n\n[COMMENT TEXT]\n", text)`
    - For nested comments (`thread_depth >= 2`):
      `full_context = paste0("[NESTED COMMENT in r/", subreddit, "]\nRoot post title: ", root_title, "\nDirect parent: ", truncate(parent_text, 300), "\n\n[COMMENT TEXT]\n", text)`
    - For unresolved (`thread_depth == NA`):
      `full_context = paste0("[COMMENT in r/", subreddit, " — parent context unavailable]\n", text)`

    Helper `truncate(text, max_chars)`: trims to max_chars with "..." appended if truncated.

    Create `scripts/02_thread_reconstruct.R`:
    ```r
    source("R/utils.R"); load_config()
    source("R/thread_reconstruct.R")

    df_filtered <- readRDS(file.path(CFG$path_processed, "01_initial_filtered.rds"))
    df_threaded <- reconstruct_threads(df_filtered)
    df_threaded <- build_full_context(df_threaded)

    saveRDS(df_threaded, file.path(CFG$path_processed, "02_with_thread_context.rds"))
    log_msg(paste("Thread reconstruction complete:", nrow(df_threaded), "records;",
                  sum(!is.na(df_threaded$parent_text)), "with resolved parent context"))
    ```
  </action>
  <verify>Run `Rscript scripts/02_thread_reconstruct.R`; `"full_context" %in% names(readRDS(...))`; spot-check 5 random comments that their `full_context` includes parent post title; log reports resolution counts</verify>
  <done>Script runs without error; all records have `full_context` field populated; comments include parent context in their full_context string; output RDS saved</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript scripts/02_thread_reconstruct.R` completes without errors
- [ ] `data/processed/02_with_thread_context.rds` exists
- [ ] `all(c("thread_id","parent_text","root_post_text","thread_depth","full_context") %in% names(df))` is TRUE
- [ ] `sum(df$thread_depth == 0, na.rm=TRUE) > 0` — root posts identified
- [ ] Manual spot-check: random comment's `full_context` contains parent post information
- [ ] Log shows thread resolution rate
</verification>

<success_criteria>
- All records have thread_id and thread_depth
- full_context field correctly includes parent context for comments
- Unresolved parents handled gracefully (NA, not error)
- Circular reference protection in place
- Executable pipeline script produces output RDS
</success_criteria>

<output>
After completion, create `.planning/phases/01-preprocessing/01-03-SUMMARY.md`:

# Phase 01 Plan 03: Thread Reconstruction Summary

**[One-liner: e.g., "Reconstructed threads for X records; Y% of comments have resolved parent context"]**

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `R/thread_reconstruct.R` — parent linking, thread ID, full_context builder
- `scripts/02_thread_reconstruct.R` — executable pipeline script

## Decisions Made
[parent_id format notes, truncation lengths chosen, edge cases encountered]

## Issues Encountered
[Problems and resolutions]

## Next Step
Ready for 01-04-PLAN.md — LLM relevance classification
</output>
