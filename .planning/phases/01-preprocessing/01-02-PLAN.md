---
phase: 01-preprocessing
plan: 01-02
type: execute
---

<objective>
Load raw Reddit CSV data, validate the schema, and apply initial structural filters: date (≥ 2020), deduplication, and minimum text length.

Purpose: Produce a clean base dataset before thread reconstruction and LLM-based filtering — cheap rule-based operations that should run first to minimise downstream API costs.
Output: `data/processed/01_initial_filtered.rds` — deduplicated, date-filtered records with validated schema.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-preprocessing/01-01-SUMMARY.md
@R/utils.R
@config/config_template.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/load_data.R — CSV ingestion with schema validation</name>
  <files>R/load_data.R</files>
  <action>
    Create `R/load_data.R` with a function `load_reddit_data(path = CFG$path_raw)` that:

    1. Scans `path` for all `.csv` and `.xlsx` files (using `list.files()` with `full.names = TRUE`)
    2. Reads each file with `readr::read_csv()` (or `readxl::read_excel()` for xlsx) with sensible defaults:
       - `col_types` loosely specified to prevent silent type coercion
       - `locale = readr::locale(encoding = "UTF-8")`
    3. Row-binds all files into a single tibble
    4. **Schema validation**: Check for presence of required columns using exact names — do NOT use flexible matching. Required columns: `parsedId`, `body`, `createdAt`, `dataType`, `parsedParentId`, `parsedPostId`, `parsedCommunityName`, `authorName`, `upVotes`, `commentUpVotes`, `title`.
       - If any required columns are missing, `stop()` with a clear message listing which are absent and which columns were found
    5. Normalise to a canonical schema using these exact mappings:
       - `id` (character) ← `parsedId`
       - `text` (character) ← `body` (contains text for both posts and comments)
       - `title` (character) ← `title` (NA for comments — do NOT fill or coerce)
       - `subreddit` (character) ← `parsedCommunityName`
       - `created_at` (POSIXct) ← `createdAt` — **NOTE: this column is only populated for posts; it will be NA for all comments. Do not attempt to parse `commentCreatedAt` as a substitute — it is not used for date filtering.**
       - `type` (character) ← `dataType` (values are exactly `"post"` or `"comment"`)
       - `parent_id` (character) ← `parsedParentId` (already bare IDs, no `t3_/t1_` prefixes; NA for root posts)
       - `post_id` (character) ← `parsedPostId` (direct link to root post; used in date filtering of comments — must be preserved)
       - `score` (integer) ← `dplyr::coalesce(upVotes, commentUpVotes)` (posts use `upVotes`, comments use `commentUpVotes`)
       - `author` (character) ← `authorName`
    6. Log count of records loaded per source file
    7. Return the combined tibble

    Do NOT filter here — just load and normalise. Filtering is in Task 2.
  </action>
  <verify>`source("R/load_data.R"); df <- load_reddit_data(); nrow(df) > 0; all(c("id","text","title","subreddit","created_at","type","parent_id","post_id","score","author") %in% names(df))`</verify>
  <done>Function loads all CSVs from data/raw/, validates schema, logs per-file counts, returns clean tibble with canonical column names</done>
</task>

<task type="auto">
  <name>Task 2: Create R/filter_initial.R — date, deduplication, and text length filters</name>
  <files>R/filter_initial.R, scripts/01_initial_filter.R</files>
  <action>
    Create `R/filter_initial.R` with a function `apply_initial_filters(df, cfg = CFG)` that applies filters in sequence, logging counts before and after each step:

    **Step 1 — Date filter (post-based, cascade to comments):**

    `created_at` is only populated for posts — comments have `NA`. The filter must therefore be applied indirectly for comments via their parent post's date.

    ```r
    # 1a. Identify valid post IDs: posts created on or after start_date
    valid_post_ids <- df |>
      filter(type == "post", as.Date(created_at) >= cfg$start_date) |>
      pull(id)

    # 1b. Keep posts in the valid set; keep comments whose root post is in the valid set
    df <- df |>
      filter(
        (type == "post"    & id      %in% valid_post_ids) |
        (type == "comment" & post_id %in% valid_post_ids)
      )
    ```

    - Comments whose `post_id` is not in `valid_post_ids` are dropped — this covers both pre-2020 parent posts AND comments whose parent post was not present in the raw data at all (e.g., scraped from outside the dataset).
    - Log: "Date filter: kept X posts (≥ {cfg$start_date}); kept Y comments linked to valid posts; dropped Z records total"

    **Step 2 — Deduplication:**
    - Remove exact duplicate `id` values (keep first occurrence)
    - Remove near-duplicate text: compute `text_hash = digest::digest(tolower(trimws(text)))` per record; remove duplicate hashes within same subreddit
    - Log: "Dedup (ID): removed X duplicates"; "Dedup (text hash): removed Y near-duplicates"

    **Step 3 — Minimum text length:**
    - Keep records where `nchar(trimws(text)) >= cfg$min_text_chars` (default 50 chars)
    - Log: "Text length filter: removed X records below {cfg$min_text_chars} chars"

    **Step 4 — Add provenance column:**
    - Add `filter_stage = "initial_pass"` and `filtered_at = Sys.time()`

    Return filtered tibble.

    Also create `scripts/01_initial_filter.R` as an executable pipeline script:
    ```r
    source("R/utils.R"); load_config()
    source("R/load_data.R")
    source("R/filter_initial.R")

    df_raw <- load_reddit_data()
    df_filtered <- apply_initial_filters(df_raw)

    saveRDS(df_filtered, file.path(CFG$path_processed, "01_initial_filtered.rds"))
    log_msg(paste("Saved:", nrow(df_filtered), "records to 01_initial_filtered.rds"))
    ```
  </action>
  <verify>Run `Rscript scripts/01_initial_filter.R`; check `data/processed/01_initial_filtered.rds` exists; `readRDS(...)` loads cleanly; all posts have `created_at >= 2020-01-01`; all comments have a `post_id` that exists in the posts present in the output; no duplicate `id` values</verify>
  <done>Script runs without error; output RDS exists; log shows record counts at each filter step; no records before 2020 remain; no duplicate IDs</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript scripts/01_initial_filter.R` completes without errors
- [ ] `data/processed/01_initial_filtered.rds` exists and is readable
- [ ] `all(df$created_at[df$type == "post"] >= as.Date("2020-01-01"), na.rm = TRUE)` is TRUE
- [ ] `all(df$post_id[df$type == "comment"] %in% df$id[df$type == "post"])` is TRUE — every comment's root post is present in the output
- [ ] `anyDuplicated(df$id) == 0` is TRUE
- [ ] `min(nchar(trimws(df$text))) >= 50` is TRUE (or whatever CFG$min_text_chars is set to)
- [ ] Console log shows filter step counts
</verification>

<success_criteria>
- load_reddit_data() handles multiple CSV/xlsx files, validates schema, normalises columns
- apply_initial_filters() applies date, dedup, and length filters with logging
- Executable pipeline script produces output RDS
- All records in output are post-2020, non-duplicate, minimum length
</success_criteria>

<output>
After completion, create `.planning/phases/01-preprocessing/01-02-SUMMARY.md`:

# Phase 01 Plan 02: Data Loading & Initial Filtering Summary

**[One-liner: e.g., "Loaded X raw records across Y files; filtered to Z records after date, dedup, and length filters"]**

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `R/load_data.R` — CSV ingestion with schema validation
- `R/filter_initial.R` — date, dedup, text-length filters
- `scripts/01_initial_filter.R` — executable pipeline script

## Decisions Made
[Column name mappings used, any schema issues encountered]

## Issues Encountered
[Problems and resolutions]

## Next Step
Ready for 01-03-PLAN.md — Thread reconstruction
</output>
